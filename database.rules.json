{
  "rules": {
    ".read": false,
    ".write": false,
    "rooms": {
      "lobby": {
        "members": {
          "$uid": {
            ".read": "auth != null",
            ".write": "auth != null && auth.uid === $uid"
          }
        }
      }
    }
  }
}
